name: Move new issues to Backlog

on:
  issues:
    types: [opened]

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.5.1
        with:
          project: StrengthJournal
          column: Backlog
          repo-token: ${{ secrets.ACCESS_TOKEN }}
